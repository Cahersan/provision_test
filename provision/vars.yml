---
apps_root: /webapps
project_name: icecreamratings_project
repository_root: "{{apps_root}}/{{project_name}}"
project_root: "{{repository_root}}/{{project_name}}"
configuration_root: "{{project_root}}/{{project_name}}"
venv_path: "{{apps_root}}/.virtualenvs/{{project_name}}"
remote_repository: https://github.com/Cahersan/icecreamratings_project.git 
#git@github.com:Cahersan/icecreamratings_project.git 
system_packages:
  - build-essential
  - vim
  - tree
  - git
  - libpq-dev
  - python-dev
  - postgresql
  - postgresql-contrib
  - nginx
  - python-setuptools
easy_install_packages:
  - virtualenv
  - pip
  - psycopg2
python_packages:
  - django 
  - gunicorn
  - psycopg2
